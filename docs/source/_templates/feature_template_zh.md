# xx特性

## 特性介绍（必选）

*一句话说明该特性的功能定义（是什么、做什么）、使用该特性后的具体收益（性能提升/容错增强/运维便捷等量化或定性收益），以及该特性引入或可用的版本。*

> **写作要求**：
>
> - （必选）一句话说明该特性的功能定义：是什么、做什么。
> - （必选）一句话说明使用该特性后的具体收益（性能提升/容错增强/运维便捷等量化收益，如"推理吞吐提升 N%""启动时间缩短 N%""故障恢复时间缩短"等）。
> - （必选）一句话说明该特性的版本能力（如"自 vLLM-Ascend x.x.x 起支持""自 MindIE x.x.x 起支持""需要 CANN x.x.x 及以上版本"等）。
> - （可选）如果需要特别体现该特性区别于其他特性的核心功能点和关键优势，可以在此列出。用表格呈现，包含：功能/优势名称、简要说明。

**示例**：

xxx 特性自 vLLM-Ascend x.x.x / MindIE x.x.x 起支持，通过 xxx 机制，在 xxx 场景下实现 xxx。可提升 xxx（如推理吞吐提升 N%、启动时间缩短 N%、故障恢复时间缩短等）。

| 核心功能 | 说明 |
|----------|------|
| 多介质感知 | 支持 NPU HBM / CPU / Disk 三级缓存命中查询，按介质权重加权评分 |
| 动态探测间隔 | 根据 NPU AI Cube 利用率动态调整探测周期（繁忙时 20s，空闲时 5s） |

### 工作原理（可选）

*特性原理：针对比较复杂、组件间交互的特性，能够用流程图体现尽量使用流程图，并在流程图下方针对各流程给出解释。*

**注：特性原理较复杂或多组件交互时必选；简单特性可在「特性介绍」中一句话说明后省略本节。**

> **写作要求**：
>
> - 尽量使用流程图（Mermaid 或 ASCII 图）体现端到端工作流程。
> - 在流程图下方针对各关键步骤给出编号解释（如"1. xxx 完成后 → 2. xxx 检测到 → 3. xxx 执行"）。
> - 涉及内部数据结构或接口时，给出简要的字段说明表格。

**示例**：

```text
┌──────────┐     step 1     ┌──────────┐     step 2     ┌──────────┐
│ Component│ ─────────────► │ Component│ ─────────────► │ Component│
│    A     │                │    B     │                │    C     │
└──────────┘                └──────────┘                └──────────┘
```

1. **xxx**：xxx。
2. **xxx**：xxx。
3. **xxx**：xxx。

*比如：自动弹性扩缩容、虚推健康探测、KV Cache 亲和性调度能力部署*

### 使用场景（必选）

*描述该特性适用的业务场景，以及各场景对应的子模式/子策略。*

**注：特性存在多种应用场景时必选；场景单一时在「特性介绍」中说明即可，省略本节。**

> **写作要求**：
>
> - 若存在多个场景，逐个列出并给出区分依据（如不同硬件、不同部署模式、不同负载特征）。说明"什么业务场景下用"（何时用），再说明该场景下采用哪种**子模式/子策略**（怎么配）。
> - 每个场景给出适用条件和不适用的边界。
> - “使用场景”章节写在特性介绍或特性使用中均可，只要有该章节即可，本模版以写在特性介绍中为例介绍。

**示例**：

该特性适用于以下场景：

| 场景 | 子模式/策略 | 适用条件 |
|------|-------------|----------|
| 场景一：xxx | 模式 A | 适用于 xxx 场景，xxx 时使用 |
| 场景二：xxx | 模式 B | 适用于 xxx 场景，xxx 时使用 |
| 场景三：xxx | 模式 C | 适用于 xxx 场景，xxx 时使用 |

### 约束与限制（必选）

> **写作要求**：
>
> - **硬件**：该特性支持的硬件产品（如 Atlas 800I A2 推理服务器、Atlas 800 A3 超节点服务器、Ascend 950 系列等）。不支持时明确说明。
> - **部署场景**：该特性支持/不支持的部署场景（如仅支持 PD 分离、仅支持 PD 混部、仅支持单机部署等）。
> - **引擎**：仅 MindIE 产品涉及，vLLM-Ascend / SGLang 不涉及。该特性支持的推理引擎（如仅 vLLM、仅 SGLang、vLLM + SGLang 均支持）。若引擎间行为有差异须说明。
> - **模型**：该特性支持/不支持的模型架构（如仅支持 Dense 模型、仅支持 MoE 模型、支持 MLA 等）。若已验证的模型清单，用表格列出。
> - **特性互斥**：该特性是否支持与其他特性同时使用（如不支持与 xx 特性同时使用）。无显式说明时默认可与其他特性共存。
> - **软件依赖**：该特性依赖的软件版本、工具、库（如需要 CRIU 3.19、需要 msprobe、需要 HDK 26.0.RC1+、CANN 版本等）。
> - **其他限制**：端口冲突、网络要求、权限要求等。
> - **方案约束**：如果特性存在一些方案、功能约束需要给出 RFC 链接，请在表格最后增加一行，说明 RFC 编号及约束内容。

**示例**：

| 类别 | 说明 |
|------|------|
| 硬件 | Atlas 800I A2 推理服务器、Atlas 800 A3 超节点服务器 |
| 部署场景 | 支持 PD 分离部署，不支持 PD 混部 |
| 引擎 | 仅支持 vLLM（仅 MindIE 产品涉及此项） |
| 模型 | 已验证 Qwen3-8B、Qwen3-30B-A3B、DeepSeek-V3.1-W8A8 |
| 特性互斥 | 不支持与 xx 特性同时使用 |
| 软件依赖 | CANN >= 8.5.0、HDK >= 25.5、Mooncake >= 0.3.11.post1 |
| 其他限制 | 需要特权容器、节点间网络需要 10Gbps+ |
| 方案约束 | RFC-001：xxx 功能受限，需参考 `http://xxx/rfc/001` |

## 特性使用

### 环境准备（可选）

*注：该特性需要前置准备时必选；直接配置即可开启的简单特性可省略。*

> **写作要求**：
>
> - 列出使用该特性前必须完成的前置步骤（如已完成基础服务部署、已安装依赖、已准备特定文件等）。
> - 给出前置步骤的交叉引用链接（如参考 [xxx 部署](../quick_start_xxx.md) 完成基础服务部署）。
> - 尽量提供软件依赖的获取来源或安装命令。

**示例**：

- 已使用 MindIE Motor 完成基础推理服务部署，服务运行正常。参考 [xxx 服务部署](../quick_start_motor.md)。
- 已安装 xxx，且版本满足 xxx 要求：`pip install xxx==x.x.x`。
- 集群网络中各节点 xxx 端口可达。

### 使用样例（必选）

*根据实际操作，介绍如何启用特性，提供明确的 step-by-step 步骤。*

> **写作要求**：
>
> - 提供 **step-by-step** 的操作步骤，每一步都给出具体命令，不跳步（如包含 `cd` 进入目录）。
> - 每个步骤给出命令代码块，并在代码块下方或通过注释解释关键参数含义。
> - 部署完成后给出**预期输出**（如日志关键字、命令输出、HTTP 响应），帮助用户确认操作成功。
>
> **多场景写法**：若「特性介绍 > 使用场景」中列出了多个场景，此处按场景拆分为子章节，每个子章节与「使用场景」中的场景一一对应。

**示例一：vLLM-Ascend（单场景，含 Online/Offline）**：

**场景一：xxx 部署**

*场景说明：对应「使用场景」中的场景一，适用于 xxx 条件。*

**Online Inference（Server Mode）**：

```bash
vllm serve Qwen/Qwen3-8B \
  --additional-config '{"xxx_config": {"enabled": true}}'
```

关键参数说明：

| 参数 | 说明 |
|------|------|
| `xxx_config.enabled` | 启用该特性，默认 false |

**Offline Inference（Python API）**：

```python
from vllm import LLM

llm = LLM(
    model="Qwen/Qwen3-8B",
    additional_config={
        "xxx_config": {
            "enabled": True,
        },
    },
)
outputs = llm.generate("Hello, how are you?")
```

**场景二：xxx 部署**

*场景说明：对应「使用场景」中的场景二，适用于 xxx 条件。*

> 写法与场景一相同，此处展示与场景一不同的配置差异即可。

**示例二：MindIE（集群部署场景）**：

**场景一：xxx 部署**

1. 修改配置文件：

   ```bash
   cd examples/deployer
   vim ../infer_engines/vllm/user_config.json
   ```

   在 `xxx_config` 中增加以下配置：

   ```json
   {
     "enable_xxx": true
   }
   ```

2. 部署服务：

   ```bash
   # 方式一：指定配置目录（推荐）
   python deploy.py --config_dir ../infer_engines/vllm

   # 方式二：单独指定配置文件
   python deploy.py --user_config_path ../infer_engines/vllm/user_config.json --env_config_path ../infer_engines/vllm/env.json
   ```

3. 确认部署结果：

   ```bash
   kubectl get pod -A -owide
   ```

   预期输出：xxx 实例和 xxx 组件均启动成功，Coordinator 日志中可看到 `xxx registered` 字样。

**场景二：xxx 部署**

> 若有不同硬件或部署模式，在此分场景写作。格式同上。

### 验证特性（必选）

*介绍如何验证特性已生效，提供明确的步骤。*

*注意将每一步操作步骤都呈现出来，避免默认用户已知某些步骤而不提供的情况，例如：进入目录的命令。*

> **写作要求**：
>
> - 提供明确的验证命令（如 `curl`、`kubectl`、日志关键字检查、API 请求）。
> - 给出预期输出或成功标志（如 HTTP 200、日志中出现特定关键字、返回包含特定字段）。
> - 每一步操作步骤都呈现出来，避免默认用户已知某些步骤。
> - 若存在多个场景且验证方式不同，按场景拆分子章节，与「使用样例」中的场景一一对应。

**示例**：

服务启动后，可通过以下方式验证特性是否生效：

1. 查看日志：

   ```bash
   kubectl logs -n mindie <coordinator-pod> | grep xxx
   ```

   预期输出：

   ```text
   xxx enabled, interval=...
   ```

2. 发送推理请求验证：

   ```bash
   curl http://<ip>:<port>/v1/completions \
       -H "Content-Type: application/json" \
       -d '{
           "model": "xxx",
           "prompt": "The future of AI is",
           "max_tokens": 50,
           "temperature": 0
       }'
   ```

   预期结果：返回 HTTP 200，响应体包含 `choices` 字段。

## 配置参数（必选）

*用表格列出特性可配置参数全景。*

> **写作要求**：
>
> - 表格包含：参数名、类型、默认值、是否必填、取值范围、说明。
> - 参数名与代码/配置文件中保持一致。
> - 若参数存在依赖关系（如 A 启用后 B 才生效），需标注。
> - 若不同产品（MindIE / vLLM-Ascend / SGLang）参数命名或配置方式不同，分产品列出。

**示例**：

| 参数 | 类型 | 默认值 | 必填 | 取值范围 | 说明 |
|------|------|--------|------|----------|------|
| `enabled` | bool | false | 是 | true/false | 启用该特性 |
| `interval` | int | 10 | 否 | 1-3600 | 探测间隔（秒） |
| `policy` | str | "default" | 否 | "default"/"aggressive" | 调度策略 |

## 调优建议（可选）

*注：特性涉及性能参数调优时必选；无调优空间的特性可省略。*

> **写作要求**：
>
> - 针对不同业务目标（如吞吐优先、时延优先、长上下文、资源受限）给出推荐配置。
> - 用表格或列表呈现，明确说明每个场景的关键参数取值建议。
> - 标注参数之间的相互影响和注意事项（如调大 A 会增加内存，需同时调小 B）。
> - 若存在性能数据，需标注测试环境硬件型号（如 A2/A3/950DT&PR 等）。

**示例**：

| 场景 | 关键参数 | 建议取值 | 说明 |
|------|----------|----------|------|
| 吞吐优先 | `param_a` | 较大值（如 64） | 提高 xxx 并发，但会增加内存占用 |
| 时延优先 | `param_b` | 较小值（如 4-8） | 降低调度延迟 |
| 长上下文 | `param_c` | 最大值 | 须配合 xxx 调整 |

> **注意**：以上配置基于特定测试环境验证，仅作参考。实际最优配置取决于 xxx 等因素，建议根据实际场景测试调优。

## 常见问题（可选）

**通用安装、环境、部署类问题请参考 [公共 FAQ](../faq.md)。以下仅收录本特性特有的疑难问题。**

> **写作要求**：
>
> - 列出该特性使用中常见的报错、异常现象。
> - 每条 FAQ 包含三要素：**问题描述**、**原因分析**、**解决步骤**。
> - 通用环境/安装类问题引用公共 FAQ 链接，不在此重复。

**示例**：

### 问题一：xxx 报错 / xxx 不生效

**问题描述**：xxx 启动后日志提示 `xxx error`，特性不生效。

**原因分析**：xxx 配置项取值与 xxx 不一致 / xxx 依赖未安装 / xxx 端口被占用。

**解决步骤**：

1. 检查 `xxx_config.xxx` 是否配置正确：`xxx`。
2. 确认 xxx 依赖已安装：`xxx --version`。
3. 检查端口占用：`netstat -tlnp | grep xxx`。

### 问题二：xxx

**问题描述**：xxx。

**原因分析**：xxx。

**解决步骤**：xxx。
